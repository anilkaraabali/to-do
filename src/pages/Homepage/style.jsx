import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() => ({
    taskBtn: {
        position: 'fixed',
        right: 16,
        bottom: 16,
        zIndex: 999
    }
}))
